<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.persist.mapper.DownloadTaskWMapper">

	<insert id="insert" parameterType="com.example.domain.DownloadTask">
	   INSERT INTO download_task
	   (id, created_at, updated_at, enabled, production_id, file_id, client_ip, expired_at, time_cost_millis)
	   VALUES
	   (default, #{createdAt}, #{updatedAt}, #{enabled}, #{productionId}, #{fileId}, #{clientIp}, #{expiredAt}, #{timeCostMillis})
	</insert>
	
	<update id="updateTimeCostMillis" parameterType="com.example.domain.DownloadTask">
	   UPDATE download_task 
	   SET time_cost_millis=#{timeCostMillis} 
	   WHERE id=#{id}
	</update>

</mapper>