<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.persist.rdbms.ProductionWMapper">

	<insert id="insert" parameterType="com.example.domain.Production">
        <selectKey order="AFTER" keyProperty="id" resultType="long">
			SELECT currval('production_id_seq')
		</selectKey>
		INSERT INTO production
		(id, created_at, updated_at, enabled, name, description)
		VALUES
		(default, #{createdAt}, #{updatedAt}, #{enabled}, #{name}, #{description})
	</insert>

</mapper>