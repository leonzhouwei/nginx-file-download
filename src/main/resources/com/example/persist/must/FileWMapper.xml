<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.persist.must.FileWMapper">

	<insert id="insert" parameterType="com.example.domain.File">
	   INSERT INTO file
	   (id, created_at, updated_at, enabled, dir, name, size, production_id)
	   VALUES
	   (default, #{createdAt}, #{updatedAt}, #{enabled}, #{dir}, #{name}, #{size}, #{productionId})
	</insert>
	
	<update id="update" parameterType="com.example.domain.File">
        UPDATE file
        SET
        name=#{name}, enabled=#{enabled},
        updated_at=#{updatedAt}
        WHERE id=#{id}
    </update>
	
	<update id="enable" parameterType="com.example.domain.File">
        UPDATE file
        SET enabled=true,
        updated_at=#{updatedAt}
        WHERE id=#{id}
    </update>
    
    <update id="disable" parameterType="com.example.domain.File">
        UPDATE file
        SET enabled=false,
        updated_at=#{updatedAt}
        WHERE id=#{id}
    </update>

</mapper>